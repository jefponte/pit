apiVersion: apps/v1
kind: Deployment
metadata:
  name: pit
spec:
  template:
    metadata:
      labels:
        environment: production
    spec:
      containers:
        - name: pit
          resources:
            requests:
              cpu: 200m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
